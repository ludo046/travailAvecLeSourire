export interface registerModel{
    firstname: String;
    lastname: String;
    age: Number;
    email: String;
    password: String;
}

export interface loginModel{
    email: String;
    password: String;
}